/* Tema del catálogo antes de pintar. Movido desde catalogo.html (v96, blindaje CSP). */
/* Tema antes de pintar (sin parpadeo). Clave propia: el cliente no comparte navegador con la app. */
try{var _t=localStorage.getItem("gcat_theme");if(_t==="dark"||(!_t&&matchMedia("(prefers-color-scheme:dark)").matches))document.documentElement.setAttribute("data-theme","dark");}catch(e){}
